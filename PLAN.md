# Feng Sync 架构与实现计划

> 调研与方案日期：2026-07-17  
> 目标：Windows 桌面 GUI；在本地文件夹、SFTP 和 Google Drive 中任意选择两个端点，执行可预览、可部分选择、冲突须人工裁决的双向差异同步。

## 1. 已确认的产品边界

- 仅支持 Windows，提供桌面 GUI。
- 仅实现双向同步，不实现镜像、单向更新、实时监控、计划任务、版本保留或限速。
- 比较后先展示完整动作列表；用户可以取消部分动作。
- 两端同时修改同一文件时必须显示冲突。所有冲突都明确选择“保留左侧”或“保留右侧”后才能开始本次同步；删除与修改之间的冲突也按此规则处理。
- 每个同步根目录都保存一份 `sync.fengdb`。它是程序内部状态，不参与普通文件同步。
- 检测文件和目录的移动/改名；无法可靠确认是移动时，安全地退化为复制加删除。
- 已完成的文件在任务重启后不重复传输。传输到一半的单文件允许重新传，但只能写入临时名称，完成并验证后再替换正式文件，不能让半成品污染目标路径。
- 预期规模小于 5000 个文件、总量小于 10 GB，但程序不设置硬限制。
- 同步普通文件、隐藏文件和空目录；不同步带 Windows `System` 属性的文件。
- 跟随符号链接、目录联接点等重解析点，同步其指向的内容。扫描器必须基于目标目录标识做环检测，避免循环遍历；链接本身不在另一端重建。
- 同步文件内容、名称和修改时间；不同步 ACL、所有者、NTFS 备用数据流、压缩/加密属性等平台元数据。
- Google Drive 原生 Docs/Sheets/Slides、快捷方式等非普通文件不参与同步。
- Google Drive 同目录重名、Windows 保留名、非法字符、尾随空格/点、路径过长或大小写折叠后重名，都作为阻塞冲突，不自动改名。

## 2. 开源项目调研结论

### 2.1 推荐直接复用：rclone

[rclone](https://github.com/rclone/rclone) 使用 MIT 许可证，同时支持本地文件系统、SFTP、Google Drive、文件列表、哈希、复制、删除和服务端移动。它最适合以随程序分发的 `rclone.exe` 形式充当统一的远程访问和传输层。

复用范围：

- SFTP 与 Google Drive 的认证、连接、列目录、读写、删除、改名和服务端移动。
- 本地与远程之间、两个远程之间的文件传输。
- 统一的 [`operations/list`、`stat` 等 RC API](https://rclone.org/rc/)，异步任务、进度和统计。
- rclone 默认使用 `.partial` 临时对象而不是直接写正式名称；不启用 `--inplace`。官方文档说明 [`--partial-suffix`](https://rclone.org/docs/#partial-suffix-string) 控制临时文件后缀。
- SFTP 支持修改时间，但精度通常只有 1 秒；只有服务器同时允许 shell 命令时才可直接取得可靠哈希，详见 [rclone SFTP 文档](https://rclone.org/sftp/)。Google Drive 则可以利用对象 ID、MD5 和服务端改名能力，详见 [Google Drive 后端文档](https://rclone.org/drive/)。

不直接使用 `rclone bisync`：

- bisync 将上次状态保存为程序本机的两份 listing，而需求明确要求两端各有 `sync.fengdb`。
- 本项目需要先生成稳定的逐项计划、允许取消部分动作，并将未选动作保留到下次比较。
- 本项目的冲突必须全部人工裁决，数据库双副本提交和中断恢复也有专门规则。
- rclone 官方将 [bisync](https://rclone.org/bisync/) 定位为高级命令，并明确提示错误使用可能导致数据丢失。因此只复用 rclone 的成熟存储后端和单项操作，不把产品状态交给 bisync。

### 2.2 只借鉴设计与测试：FreeFileSync

[FreeFileSync 双向同步说明](https://freefilesync.org/manual.php?topic=synchronization-settings)确认其核心也是“三方比较”：读取两端当前状态，并用 `sync.ffs_db` 中的上次同步状态判断创建、更新和删除方向；它也会在文件系统有稳定 ID 时识别移动。

借鉴内容：

- “比较—人工检查—同步”的 GUI 工作流。
- 以上次共同状态为基准判断删除，而不是把“某侧不存在”直接当成新增或删除。
- 动作列表中的方向、冲突、排除和移动可视化。
- 先复制、后删除、失败安全替换和文件夹锁等可靠性原则。

不复制或链接其代码，也不依赖其安装包。FreeFileSync 没有为第三方产品提供稳定 SDK，其官网当前还对安装包再分发和商业使用施加额外条件；直接复用既不省集成成本，也会增加授权风险。

### 2.3 只借鉴同步语义：Unison

[Unison](https://github.com/bcpierce00/unison) 是成熟的双副本同步器，具有上次状态 archive、明确的冲突模型和良好的异常恢复设计。可借鉴它的三方比较语义、属性差异处理和状态机测试思路。

不作为实现基础：它是 GPLv3、主体为 OCaml，远程模式依赖对端 Unison/SSH，不提供 Google Drive 存储抽象，也不适合嵌入 WPF 应用。

### 2.4 不采用：Syncthing

[Syncthing](https://github.com/syncthing/syncthing) 的 MPLv2 许可比 GPL 宽松，文件索引、版本向量和防丢失原则值得参考；但它解决的是多个在线设备之间的持续 P2P 同步，不是本地/SFTP/Google Drive 两端的人工预览式任务。引入其设备协议、发现、网络和数据库会增加大量无关复杂度。

### 2.5 总结

最省力且稳定的组合是：**自研小型三方差异规划器和状态数据库，rclone 负责所有远程协议与实际传输，FreeFileSync/Unison 仅作为行为和测试参考。** 不重新实现 SFTP、Google OAuth、Drive 上传、服务端移动等高风险基础设施，也不把本项目特有的状态语义强塞给现有同步命令。

## 3. 推荐技术栈

推荐 **C# + .NET 10 LTS + WPF**：

- 项目只面向 Windows，WPF 是 .NET 自带且持续维护的 Windows 桌面框架，具备成熟的数据绑定、虚拟化表格、无障碍和打包工具；参见 [WPF 官方概览](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/overview/)。
- 截至调研日期，[.NET 10 是活跃支持的 LTS](https://dotnet.microsoft.com/en-us/platform/support/policy)，支持到 2028-11-14。
- 相比 Go/Wails 或 Rust/Tauri，不需要额外 WebView 前端、JS 工具链或跨语言边界。这个项目的难点是同步状态正确性，而不是跨平台 UI，C# 能用更少的工程层实现 GUI、进程控制、SQLite、Windows 文件属性和 Win32 文件 ID。

建议依赖保持精简：

- `CommunityToolkit.Mvvm`：MVVM、异步命令和可观察状态，微软维护，适用于 WPF；[官方说明](https://learn.microsoft.com/en-us/dotnet/communitytoolkit/mvvm/)。
- `Microsoft.Data.Sqlite`：直接使用 SQLite，不引入 ORM；[官方说明](https://learn.microsoft.com/en-us/dotnet/standard/data/sqlite/)。
- `rclone.exe`：固定并测试一个明确版本，连同 MIT 许可证一起分发；升级必须重新运行完整集成测试，不跟随最新版自动漂移。

发布采用 win-x64 self-contained 构建，把 .NET 运行时和经过验证的 rclone 一并放入安装包，用户无需单独安装依赖。微软文档确认 [self-contained 发布](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/publish-first-app)可包含运行时。

## 4. 总体架构

```text
WPF GUI / MVVM
    │
    ├── 配置与凭据管理
    ├── 比较结果/冲突裁决/部分选择
    └── 进度、错误和恢复提示
            │
Application 层
    ├── CompareCoordinator   扫描并生成不可变计划
    ├── ConflictResolver     验证所有冲突已有人工选择
    ├── SyncExecutor         按安全顺序执行所选动作
    └── RecoveryCoordinator  读取日志、验证并继续中断任务
            │
Domain 层（不依赖 UI/rclone/SQLite）
    ├── Endpoint / EntrySnapshot / Fingerprint
    ├── ChangeDetector / MoveDetector
    ├── ThreeWayPlanner / Conflict
    └── SyncPlan / SyncOperation / 状态机
            │
Infrastructure 层
    ├── LocalScanner（Windows 属性、文件 ID、重解析点环检测）
    ├── RcloneClient（SFTP、Drive、传输、远程 stat/move/delete）
    ├── StateDatabase（sync.fengdb 的本地缓存与发布）
    ├── TaskJournal（本机任务级断点状态）
    └── CredentialStore（rclone 配置 + Windows DPAPI）
```

Domain 层必须可以完全在内存和临时目录中测试。UI 不直接拼 rclone 参数，也不直接修改数据库；所有破坏性操作都只能来自已经冻结并显示过的 `SyncPlan`。

## 5. 端点与 rclone 集成

### 5.1 端点模型

每个端点配置保存：

- `endpointId`：本机配置的稳定 UUID。
- 类型：Local、SFTP、GoogleDrive。
- 规范化根位置；SFTP 包括主机指纹，Drive 包括账号/Drive 标识和根文件夹 ID。
- 能力：哈希种类、修改时间精度、服务端移动、空目录、稳定对象 ID、大小写敏感性。

同步对使用无顺序的 `pairId` 绑定两个端点的稳定身份，不能只按用户输入的路径字符串绑定。检测到账号、服务器指纹或 Drive 根 ID 改变时，必须阻止沿用旧数据库。

### 5.2 rclone 运行方式

- 应用启动一个只监听 `127.0.0.1` 随机端口的 `rclone rcd` 子进程。
- 每次生成随机 RC 用户名和密码；子进程随 GUI 退出，禁止监听局域网地址。
- 通过 JSON RC API 列目录、stat、创建目录、复制、移动和删除；异步 job 提供可取消任务和进度。
- rclone 配置放在 `%LocalAppData%\FengSync\rclone\rclone.conf`。配置密码由随机密钥加密，密钥使用当前 Windows 用户范围的 [DPAPI](https://learn.microsoft.com/en-us/dotnet/standard/security/how-to-use-data-protection)保护；明文密码不写日志和命令行。
- Google OAuth 交给 rclone 的授权流程；SFTP 必须保存并校验 SSH 主机指纹，首次信任和指纹变化均由用户明确确认。
- 所有 rclone 日志转成结构化内部事件；对路径、账号、token、密码和连接串做脱敏。

### 5.3 扫描策略

- 本地端点由 `LocalScanner` 使用 Windows API 枚举，以取得 `System`/`Hidden` 属性、稳定文件 ID、精确修改时间和重解析点目标；遇到 `System` 条目整项排除。
- 跟随重解析点时，以“卷序列号 + 目录文件 ID”记录已访问目录。再次遇到同一目录即停止该分支并在比较结果中显示环警告。逻辑路径仍使用同步根下的链接路径，不能把目标的绝对路径泄露到另一端。
- SFTP 和 Drive 使用 rclone 递归列表。扫描结果统一为相对路径、类型、大小、修改时间、可用哈希、稳定 ID 和端点能力。
- `sync.fengdb`、数据库临时文件、锁文件和 Feng Sync 的 `.partial` 文件始终排除。
- 路径先按端点原始名称保存，再生成 Windows 兼容键和大小写折叠键。任意两个远程条目映射到同一个 Windows 键时，产生阻塞冲突。

## 6. `sync.fengdb` 设计

### 6.1 文件角色

`sync.fengdb` 使用 SQLite，两个端点保存内容相同的已提交快照。数据库只表示“最后一次成功提交的同步状态”，不在远程位置直接打开：先下载到 `%LocalAppData%\FengSync\cache\<pairId>`，校验后在本地读写，关闭 SQLite 后再发布回两端。

不要在远程或网络文件系统上使用 WAL。SQLite 官方说明 [WAL 依赖共享内存，不支持多机网络文件系统](https://sqlite.org/fileformat.html)。本地工作副本使用默认 rollback journal；发布前执行 checkpoint/关闭连接并运行 `PRAGMA quick_check`，确保最终只有一个完整数据库文件。

### 6.2 基本表

- `meta`：`schema_version`、`application_id`、`pair_id`、`generation`、`parent_generation`、两个端点身份摘要、创建时间、最后成功时间、清单摘要。
- `entries`：规范化相对路径、条目类型，以及左右两侧各自的存在状态、大小、修改时间、哈希类型/值、稳定对象 ID、Windows 属性、最近确认 generation。
- `tombstones`：最近删除或移动的身份信息，保留少量 generation 供删除传播、移动识别和中断恢复，之后清理。
- `commit_info`：本次提交关联的本机任务 ID、已执行计划摘要和数据库内容摘要。

两侧快照必须分别保存，不能假设同步后两端元数据完全一致：SFTP 修改时间精度、Drive 元数据以及用户取消的动作都会使两边的已知状态不同。

### 6.3 双副本一致性

跨两个端点不可能做真正的原子事务，因此用 generation 和可恢复发布协议：

1. 启动比较时下载两份数据库并验证 SQLite 完整性、`pair_id`、端点身份、generation 和内容摘要。
2. 正常情况要求两份数据库完全相同。首次同步则要求两边都没有数据库；若只有一边有库，不擅自判断是首次还是意外丢失。
3. 同步文件全部成功并复核后，在本地生成 `generation + 1` 的新库并关闭连接。
4. 分别上传为随机临时名，下载/哈希复核，再通过端点内改名替换 `sync.fengdb`。rclone 保持非 `--inplace` 模式。
5. 若只替换成功一侧便中断，下次根据本机任务日志、`parent_generation`、计划摘要和实际文件复核，能证明新 generation 完整时修复旧侧；不能证明时停止并要求用户处理，绝不按“数字更大”自动选择。

首次建立、数据库缺失、数据库损坏、pair 不匹配、两份库同 generation 但摘要不同，都必须在 GUI 中单独说明。禁止静默重建，否则会把删除误判为新增。

## 7. 差异、删除、冲突与移动算法

### 7.1 三方比较

输入是：左侧当前扫描 `L1`、右侧当前扫描 `R1`、数据库中的上次左右快照 `L0/R0`。

- 分别比较 `L1↔L0` 和 `R1↔R0`，得到未变、新建、修改、删除、移动。
- 仅一侧改变：把变化传播到另一侧。
- 两侧都改变且最终内容可证明相同：视为已自行合流，不报冲突。
- 两侧都改变但结果不同：冲突。
- 一侧删除、另一侧未变：传播删除。
- 一侧删除、另一侧修改或移动：冲突，由用户选择“保留文件”或“保留删除”。
- 首次同步没有基线：只在一侧存在的条目复制到另一侧；同路径两侧不同则冲突；绝不推断删除。

比较先使用大小、修改时间精度和稳定 ID 做快速筛选。只要方向或相等性存在歧义，就请求双方共同支持的强哈希；SFTP 无远程哈希能力时按需流式读取或下载到本机临时缓存计算，不用“同大小同时间”替代内容证明。

### 7.2 移动检测

移动检测在逐路径规划之前完成：

1. 优先匹配稳定对象 ID：本地 Windows 文件 ID、Drive 对象 ID。
2. 没有稳定 ID 时，只在“旧路径消失 + 新路径出现”的候选集中，以类型、大小和强哈希匹配。
3. 一个指纹对应多个候选时不猜测，退化为复制加删除并在预览中说明。
4. 目标端支持同端服务端移动时生成 Move；否则生成 Copy + Verify + Delete。
5. 大小写改名、互换名称和移动环使用唯一临时名称拆环，确保不会覆盖中间文件。

### 7.3 部分选择的状态规则

- 用户取消的新增、更新、删除或移动不写成“已同步”，数据库继续保留原基线，使它下次仍出现在比较结果中。
- 只有执行成功且复核通过的动作才更新相应两侧快照。
- 未改变项目可刷新非语义性观测值，但不得改变用于删除/冲突判断的基线。
- 冲突即使不勾选普通同步动作，也必须明确选择保留左或右；未裁决冲突存在时禁用“开始同步”。

## 8. 安全执行与任务级恢复

比较完成后生成不可变 `SyncPlan`，每项包含源/目标、预期源指纹、预期目标指纹、动作、依赖、用户选择和唯一操作 ID。开始前重新 stat 涉及的路径；任何状态变化都使计划过期，返回重新比较，不能执行陈旧计划。

执行顺序：

1. 连接、数据库和端点身份预检，取得同步对锁。
2. 创建目标目录。
3. 执行无覆盖风险的移动；移动环通过临时名拆解。
4. 复制新增和更新文件到目标临时名。
5. 校验临时文件大小；双方支持共同哈希时再校验哈希。随后原子/服务端改名到正式路径。
6. 重新检查源文件未在传输期间改变；改变则将该项标为失败，不删除任何旧数据。
7. 只有所有前置复制和移动成功后才执行文件删除，最后自底向上删除空目录。
8. stat 或局部重扫所有受影响路径，生成新数据库并按双副本协议提交。
9. 释放锁并保留可读日志。

本机 `%LocalAppData%\FengSync\jobs` 保存任务 journal，而不是把进行中的事务写入两端已提交数据库。每项状态为 Pending、Running、Transferred、Verified、Committed 或 Failed，并在每次状态转换后持久化。

应用或网络中断后：

- 删除残留且不再被运行任务引用的临时文件。
- 对 journal 中的 Transferred/Verified 项重新 stat/哈希；符合预期则跳过传输，不符合则重新复制整个单文件。
- 已完成正式替换的文件不重复传。
- 任意复制失败时，本轮不执行尚未开始的删除，也不提交新 generation。
- “取消”停止调度新动作并等待当前 rclone 操作安全退出；不能强杀到留下正式名称的半成品。

## 9. GUI 基本结构

单窗口即可完成主要流程：

- 顶部左右端点卡片：类型、路径/远程、连接状态、配置和交换左右位置。
- “比较”按钮：扫描两端和数据库，生成结果但不修改文件。
- 中部虚拟化树形表格：相对路径、左侧大小/时间、动作方向、右侧大小/时间、选择框、原因。支持按新增、修改、删除、移动、冲突、错误筛选。
- 普通动作可以逐项或按目录勾选；父子选择采用明确的三态逻辑。
- 冲突行使用独立的“保留左侧/保留右侧”按钮，不用普通勾选框代替裁决。非法 Windows 路径、Drive 重名和不支持对象也作为阻塞冲突显示原因。
- “开始同步”只在扫描仍有效、所有冲突已裁决且至少选中一个动作时启用。
- 执行页显示总体和当前文件进度、速度、已完成/失败数量、取消按钮及可展开日志。
- 完成页明确区分成功、部分失败、已取消和数据库提交失败，并提供重新比较。

UI 只展示 Domain 层产生的类别和原因，不自行推断同步方向。

## 10. 必须覆盖的可靠性测试

虽然不设置项目里程碑，但以下测试是实现完成的必要条件：

- 三方比较表驱动测试：新增、修改、删除、双改、删改冲突、双方得到相同内容、部分取消、首次同步。
- 移动测试：文件/目录移动、同内容多候选、大小写改名、名称互换、Drive ID、本地文件 ID、SFTP 哈希退化。
- 路径测试：Windows 保留名、非法字符、尾点/空格、超长路径、Unicode、大小写碰撞、Drive 同名对象。
- 重解析点测试：文件链接、目录联接点、链接到根外、链接环、多条链接指向同一目录；确认无无限递归且逻辑路径稳定。
- 文件属性测试：隐藏文件被同步，System 文件被排除，空目录被创建/删除，ACL/ADS 不参与比较。
- 故障注入：在每个复制、验证、移动、删除、第一侧 DB 发布、第二侧 DB 发布前后模拟崩溃、断网、磁盘满和权限拒绝。
- 恢复测试：完成项不重传；半文件只以临时名存在；失败时删除未提前执行；两份 DB generation 分叉可证明时修复、不可证明时阻止。
- 集成测试：本地↔本地、Windows OpenSSH SFTP↔本地、Drive 测试账号↔本地，以及 SFTP↔Drive。
- rclone 版本验收：固定版本的列表字段、哈希、临时文件、覆盖、服务端移动、取消行为和错误码均建立回归测试。
- 状态机随机测试：随机生成两侧文件树和操作序列，验证“没有未裁决覆盖”“删除必有基线依据”“失败不提交”“成功后再次比较无动作”等不变量。

## 11. 实现中不可破坏的不变量

- 没有上次基线时不传播删除。
- 未经用户裁决，不覆盖双侧都改变的文件。
- 正式目标路径永远不暴露半文件。
- 复制未验证成功前不执行其依赖删除。
- 未选择和失败的动作不伪装成已同步。
- 两份数据库不能被静默择优或重建。
- Domain 层只生成计划，执行层只执行冻结计划；执行时发现现实状态变化必须重新比较。
- rclone 是可替换的传输适配器，不是同步决策者。未来即使更换传输库，数据库格式、差异算法和 GUI 行为也不应被迫重写。

## 12. 最终建议

不要从 FreeFileSync、Unison 或 Syncthing fork，也不要直接包一层 `rclone bisync`。最合适的实现是一个较薄的 Windows 原生产品层：用 .NET 10/WPF 完成预览和人工冲突工作流，用独立且充分测试的三方规划器保证删除与部分同步语义，用 SQLite 双副本记录最后已提交状态，用本机 journal 完成任务级续跑，所有协议与文件搬运交给固定版本的 rclone。

该划分把最容易出错、又最符合本项目特点的部分——状态、冲突、数据库提交和恢复——控制在少量自研代码内，同时最大限度复用 rclone 已经长期维护的 SFTP、Google Drive、OAuth、哈希、临时上传和服务端移动能力。
